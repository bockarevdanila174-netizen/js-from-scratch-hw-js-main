// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое

function calculateAverage(oneNumber,twoNumber) {
    let average = (oneNumber + twoNumber) / 2
    return average
}
console.log(calculateAverage(5,3))
